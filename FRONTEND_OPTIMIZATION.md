# software_chian 前端优化建议

> 目标：提升安全运营台可用性、信息密度与专业感，同时保持「深色安全控制台」气质。  
> 范围：主站 `index.html` / `static/css/style.css` / `static/js/app.js`，以及 `dashboard.html` 监控中心。

---

## 一、现状简评

### 做得好的地方
- 深色主题变量体系清晰（GitHub 风格 token），严重等级配色一致
- 主流程完整：资产展示 → 扫描入口 → 进度 → 结果（汇总 / AI / 表格 / 详情 Tab）
- FOFA 风格搜索条、资产卡片、优先级事件等已有雏形
- dashboard 有节点监控、实验列表、研究大脑、对话入口

### 主要问题（前端重点）
1. **双页面割裂**：主站与 `dashboard.html` 视觉、导航、交互不一致，像两个产品
2. **信息架构偏「表单驱动」**：核心价值是情报与风险，却把「输入 URL」放在主视线
3. **dashboard 内联样式过多**：难以维护，且与主站 CSS 变量体系脱节
4. **缺少统一导航 / 侧边栏**：功能多了以后会迷路
5. **空状态与加载态较弱**：大量「加载中...」文字，缺少骨架屏与引导
6. **表格信息密度高但可操作性弱**：缺少列配置、排序、批量操作、详情抽屉
7. **移动端可用但不够专业**：安全控制台以桌面为主，但侧栏 / 表格需更好适配
8. **无设计源文件**：后续改 UI 缺少 Figma 基准，协作成本高

---

## 二、信息架构建议（优先做）

建议统一成「一个壳 + 多视图」：

```
顶部栏：产品名 | 环境状态(AI/FOFA) | 角色 | 登录
侧边栏：
  - 概览 / 情报墙（原 showcase + 研究大脑摘要）
  - 扫描任务（原 query + progress）
  - 资产库（FOFA 风格列表）
  - 风险与漏洞（按 CVE / 资产聚合）
  - 大脑监控（原 dashboard）
  - 设置（Key、密码、节点）
```

短期可先不做路由框架，用「同一页分区 + 锚点 / Tab」过渡；中期再拆 SPA 或 Flask 多模板共享 layout。

---

## 三、主站前端优化清单

### 1. 导航与布局
- [ ] 增加左侧固定侧栏（宽 220–240px），主内容区自适应
- [ ] 顶部栏精简：只保留品牌、全局状态徽章、用户区
- [ ] 将「只读 / 管理员」权限提示做成更明确的 Banner（未登录时锁定扫描区）

### 2. 情报墙（Showcase）优先
- [ ] 把「网络资产与供应链情报」作为登录后默认首页
- [ ] 顶部 KPI 卡片：资产总数 / 严重漏洞 / 待验证凭据 / 研究项目数
- [ ] 搜索条固定，支持语法提示（FOFA 常用语句 chips）
- [ ] 资产卡片：左侧风险点 + 主机，右侧技术标签与漏洞计数；展开改为右侧 **Drawer** 而非行内撑开（减少跳动）

### 3. 扫描任务区
- [ ] 模式 Tab 保留，但默认「FOFA」或根据配置智能默认
- [ ] 扫描选项改为「高级选项」折叠，减少首屏噪音
- [ ] 进度条旁展示当前阶段时间线（发现 → 指纹 → 漏洞 → AI）
- [ ] 取消按钮与「后台继续」语义区分清楚

### 4. 结果区
- [ ] 汇总卡片加环状风险分或简易仪表（纯 CSS 即可）
- [ ] 严重等级分布：考虑横向堆叠条 + 百分比，避免仅数字
- [ ] 资产表：固定表头、列宽拖拽（可选）、风险列固定右侧
- [ ] 详情 Tab 改为 **右侧抽屉 + 子 Tab**，避免表格行展开导致滚动位置丢失
- [ ] AI 分析面板：攻击链用垂直时间线；修复计划表支持按 P0/P1 过滤

### 5. 交互与反馈
- [ ] 统一 Toast（成功 / 失败 / 配额不足）
- [ ] 骨架屏：KPI、资产列表、表格三处
- [ ] 空状态插画 + 一句话引导（「先配置 FOFA Key」/「下发一次扫描」）
- [ ] 危险操作二次确认（取消任务、导出敏感证据）

### 6. 工程结构
- [ ] 删除根目录重复的 `style.css` / `app.js` / `index.html`（以 `static/` + `templates/` 为准）
- [ ] `dashboard.html` 拆出公共 CSS（复用 `:root` 变量）与 `dashboard.js`
- [ ] 建立设计 token 文档（颜色、圆角、字号）与 Figma Variables 对齐

---

## 四、Dashboard（大脑监控中心）优化

### 布局
- [ ] 与主站同一套顶栏 + 侧栏
- [ ] 节点卡片：在线优先排序；离线半透明
- [ ] 图表区：引入轻量图表库（ECharts / Chart.js）替代纯文字进度条，提升「监控感」
- [ ] 对话区固定在右下角浮层或独立面板，避免占据主流程过长

### 资产表
- [ ] 与主站资产库组件复用（同一套列定义与筛选）
- [ ] 支持按协议 / 国家 / 项目筛选

### 研究大脑
- [ ] 热门组件与趋势信号用「热度条 + 排名」统一组件
- [ ] 项目部署卡片显示「上次运行时间」与「下次计划」

---

## 五、视觉与品牌

| 元素 | 建议 |
|------|------|
| 主色 | 保持 `#58a6ff`，强调色可加安全绿 `#3fb950` 与警报红 `#f85149` |
| 背景 | `#0d1117` / `#161b22` 层级保留，卡片 hover 仅提边框亮度 |
| 字体 | 正文系统字体；代码/IP 统一 `SF Mono` / `JetBrains Mono` |
| 圆角 | 统一 6 / 8 / 12，避免混用 |
| 图标 | 用一套线性图标（Lucide / Phosphor），替换 emoji 作主图标（可保留少量装饰） |
| Logo | 自定义盾牌/链路 SVG，替代 emoji |

---

## 六、Figma 设计稿建议（界面图清单）

请在 Figma 中按 **Desktop 1440** 优先绘制以下帧（Mobile 可选）：

1. **Overview / 情报墙** — KPI + 搜索 + 资产卡片列表  
2. **Scan Task** — FOFA / URL 双模式表单 + 进度时间线  
3. **Asset Detail Drawer** — 技术栈 / 供应链 / 漏洞 / 利用 / API  
4. **AI Analysis** — 攻击链时间线 + 修复计划表  
5. **Brain Monitor** — 节点网格 + 实验列表 + 对话浮层  
6. **Login Modal** — 与当前深色风格一致  
7. **Empty / Loading / Error** 三态组件  

### 设计规范页
- Colors（与 CSS 变量一一对应）
- Typography
- Components：Button、Badge、Tag、Card、Table、Drawer、Toast、Sidebar Item

### 如何与代码协作
1. 在 Figma 建文件「software_chian UI」
2. 用 Variables 映射 `--bg-primary`、`--accent-blue` 等
3. 开发按帧导出标注或 Dev Mode
4. 本仓库 `docs/design/` 下可保留截图与本 MD 的链接

> **说明**：当前 Figma 账号 seat 为 View，可能无法通过 API 直接创建/写入设计文件。请在 Figma 网页端用可编辑权限账号建文件后，把链接发回，即可继续用工具生成界面帧。

---

## 七、实施优先级（建议 2 周节奏）

### P0（3–5 天）
1. 统一 layout（顶栏 + 侧栏骨架）
2. 资产详情改为抽屉
3. 去掉重复静态文件，dashboard 抽出公共 CSS
4. 空状态 + Toast

### P1（1 周）
1. 情报墙 KPI + 搜索 chips  
2. 扫描进度时间线  
3. 表格固定表头与风险筛选  
4. Figma 出 1–3 关键帧并对齐 token  

### P2
1. ECharts 监控图  
2. 轻量 SPA 或模板继承  
3. 无障碍与键盘操作完善  

---

## 八、验收标准（前端）

- [ ] 未登录用户 3 秒内理解「只读情报」与「登录可扫」
- [ ] 资产从列表到详情 ≤ 2 次点击，滚动位置不跳飞
- [ ] 主站与 dashboard 视觉同源（颜色、圆角、按钮）
- [ ] 1440 与 1280 宽度下侧栏+内容无横向挤压错乱
- [ ] 无内联大段样式残留在 dashboard 关键逻辑中

---

## 九、相关路径

| 路径 | 说明 |
|------|------|
| `templates/index.html` | 主站模板（优先维护） |
| `static/css/style.css` | 主样式 |
| `static/js/app.js` | 主逻辑 |
| `dashboard.html` | 监控中心（建议迁入 templates + static） |
| 本文件 | `FRONTEND_OPTIMIZATION.md` |

---

*文档生成日期：2026-08-13 · 针对 tajleonbennis-maker/software_chian*
